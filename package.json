{
  "name": "threadvet",
  "version": "1.0.0",
  "private": true,
  "description": "ThreadVet: free reseller profit calculator and marketplace fee guide.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "serve": "node scripts/build.mjs && node scripts/serve.mjs",
    "test": "node --test \"test/*.test.mjs\"",
    "test:e2e": "node --test --test-concurrency=1 \"test/e2e/*.e2e.mjs\""
  },
  "license": "UNLICENSED"
}
